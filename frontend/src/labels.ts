// Shared UI-language mappings for enum values coming from the backend, so the
// feature list, detail panel and PDF report all show the same German words.

export const RISK_LABEL: Record<string, string> = {
  low: "niedrig",
  medium: "mittel",
  high: "hoch",
};
